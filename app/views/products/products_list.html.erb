<p id="notice"><%= notice %></p>

<h2>Products of: <i>"<%=@category.name%>"</i> Category</h2>
<table>
  <thead>
    <tr>
      <th>Name</th>
      <th>Proce</th>
      <th> edit </th>
    </tr>
  </thead>

  <tbody>
    <% @products.each do |product| %>
      <tr>
        <td><%= product.name %></td>
        <td><%= product.price %></td>
        <td><%= link_to 'Edit', edit_product_url(product) %></td>
      </tr>
    <% end %>
  </tbody>
</table>